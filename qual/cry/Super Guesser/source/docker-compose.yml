version: '3'
services:
  hacktod:
    container_name: Super Guesser
    restart: always
    build:
      context: .
      args:
        binary: server.py
    ports:
      - "18005:5000" # exposed:local
    read_only: true